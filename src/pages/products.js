import { productCatalog } from '../data/demo-data.js';
export function renderProducts(){return `<section><div><div class="text-sm text-blue-600 font-bold">PRODUCT DATABASE</div><h1 class="text-3xl font-black mt-1">Product catalog</h1><p class="text-slate-500 mt-2">Demo product intelligence and risk profile.</p></div><div class="grid md:grid-cols-2 xl:grid-cols-3 gap-5 mt-7">${productCatalog.map(p=>`<div class="bg-white rounded-2xl shadow-soft border p-5"><div class="h-36 rounded-xl bg-gradient-to-br from-slate-100 to-blue-50 grid place-items-center text-5xl">📦</div><div class="flex justify-between gap-3 mt-4"><h2 class="font-black">${p[0]}</h2><span class="text-xs font-bold ${p[4]==='Critical'?'text-red-600':p[4]==='High'?'text-orange-600':'text-emerald-700'}">${p[4]}</span></div><div class="text-sm text-slate-500 mt-1">${p[1]} · ${p[2]}</div><div class="flex justify-between mt-4 text-sm"><span>Compliance score</span><b>${p[3]}/100</b></div><div class="h-2 bg-slate-100 rounded-full mt-2"><div class="h-2 bg-blue-600 rounded-full" style="width:${p[3]}%"></div></div><button data-product-name="${p[0]}" class="mt-4 w-full border rounded-xl py-2.5 text-sm font-bold hover:bg-slate-50">Inspect product</button></div>`).join('')}</div></section>`;}
export function initProducts({go,selectDemo}){document.querySelectorAll('[data-product-name]').forEach(b=>b.onclick=()=>{go('scan');const name=b.dataset.productName;const key=name.includes('Flour')?'flour':name.includes('Soap')?'soap':name.includes('Oil')?'oil':'flour';setTimeout(()=>selectDemo(key),0);});}
