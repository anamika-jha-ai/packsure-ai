import { rules } from '../data/rules.js'; import { toast } from '../utils/ui.js';
export function renderRules(){return `<section><div><div class="text-sm text-blue-600 font-bold">LEGAL METROLOGY RULES ENGINE</div><h1 class="text-3xl font-black mt-1">Rules</h1><p class="text-slate-500 mt-2">Demonstration mapping between categories and packaging requirements.</p></div><div class="rounded-xl border border-amber-200 bg-amber-50 text-amber-900 p-4 mt-6 text-sm"><b>Demonstration rule set:</b> final implementation should be validated against current official Legal Metrology regulations.</div><div class="grid md:grid-cols-2 xl:grid-cols-3 gap-5 mt-5">${rules.map((r,i)=>`<button data-rule="${i}" class="text-left bg-white rounded-2xl shadow-soft border p-5 hover:border-blue-300"><div class="h-10 w-10 rounded-xl bg-blue-50 text-blue-700 grid place-items-center font-black">✓</div><h2 class="font-black mt-4">${r[0]}</h2><div class="text-xs text-blue-600 font-bold mt-2">${r[1]}</div><p class="text-sm text-slate-500 mt-2">${r[2]}</p><div class="mt-4 text-xs font-bold text-emerald-700">ACTIVE · DEMO RULE</div></button>`).join('')}</div></section>`;}
export function initRules(){document.querySelectorAll('[data-rule]').forEach(b=>b.onclick=()=>{const r=rules[b.dataset.rule];toast(`${r[0]} · IF required = TRUE AND detected value = NULL → violation = TRUE`);});}
